[build]
  base    = "app"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — every unknown path serves index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
